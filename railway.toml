[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/init_db.py && python main.py run"
restartPolicyType = "always"
restartPolicyMaxRetries = 10

[[deploy.volumeMounts]]
mountPath = "/app/data"
name = "channelforge-data"
