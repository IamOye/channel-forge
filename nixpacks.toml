[phases.setup]
nixPkgs = ["ffmpeg", "python311", "dejavu_fonts", "liberation_ttf"]

[phases.install]
cmds = [
    "python3.11 -m venv --copies /opt/venv",
    ". /opt/venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = "python3.11 main.py run"
